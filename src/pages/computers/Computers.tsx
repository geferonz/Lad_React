import Books from 'components/books/Books';

const Computers = () => {
  const title='Computers';
  const src='COMPUTERS';
  return (
    <Books title={title} src={src} />
  );
}

export default Computers;
